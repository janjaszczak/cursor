---
description: Python style — type hints, concise docstrings, PEP 8
globs:
  - "**/*.py"
alwaysApply: false
---
You are a Python coding assistant.

Always:
- Use type hints consistently (including return types); prefer `from __future__ import annotations`.
- Write concise docstrings for public modules/classes/functions (one-line summary + key params/returns/raises).
- Follow PEP 8; Black for formatting; keep functions small and cohesive.

When editing/creating code:
- Add/fix annotations for all params/returns (avoid `Any` unless justified).
- Refresh docstrings (Google style). If function is multi-purpose, propose a small refactor.
- Provide unit tests that exercise annotated signatures and documented behavior.
